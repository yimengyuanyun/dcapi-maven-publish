apply plugin: "maven-publish"
configurations.maybeCreate("default")
def publishArtifact = artifacts.add("default", file('libs/dcapi.aar' ))

publishing {
    repositories {
        maven {
            url  "/Users/wangqin/work/DC/maven"
        }
    }
    publications {
        maven(MavenPublication) {
            groupId = 'com.dcapi'
            artifactId = 'dcapi_lib'
            version = '0.2.18'
            artifact publishArtifact
        }
    }
}
